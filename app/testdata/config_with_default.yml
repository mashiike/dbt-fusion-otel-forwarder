exporters:
  otlp:
    type: otlp
    endpoint: "${OTLP_ENDPOINT:-http://localhost:4317}"
    headers:
      "Authorization": "Bearer ${API_KEY}"

forward:
  default:
    traces:
      exporters: [otlp]
